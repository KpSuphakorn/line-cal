# Pinned exactly so a rebuild deploys the same code that was tested. These are
# the versions the suite runs against; bump them deliberately, not by rebuilding.
fastapi==0.141.1
uvicorn[standard]==0.53.0
line-bot-sdk==3.25.0
google-generativeai==0.8.6
pydantic==2.13.5
pydantic-settings==2.15.0
sqlalchemy==2.0.54
pillow==12.3.0
python-dotenv==1.2.3
httpx==0.28.1
python-multipart==0.0.32
psycopg2-binary==2.9.13
pytest==9.1.1
alembic==1.20.0
