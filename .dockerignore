# Keep every local secrets file out of the image; the checked-in template is
# the one exception, and it holds placeholders only.
.env*
!.env.example
.venv
__pycache__
*.py[cod]
*.db
.pytest_cache
.git
tests
node_modules
agent-runs
